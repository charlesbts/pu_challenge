<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="300dp"
    android:layout_marginBottom="12dp"
    android:outlineProvider="paddedBounds"
    android:elevation="6dp"
    android:clickable="true"
    android:focusable="true"
    android:background="#FFFFFF"
    android:foreground="?android:attr/selectableItemBackground"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/dealImageView"
        android:layout_width="match_parent"
        android:scaleType="fitXY"
        android:layout_height="200dp"
        android:transitionName="profile"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/dealTitleTextView"
        android:text="Test Title"
        android:maxLines="1"
        android:textSize="14sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dealImageView"/>

    <TextView
        android:id="@+id/dealDescriptionTextView"
        android:text="Test Description long long long long long long long long long long long"
        android:maxLines="2"
        android:textSize="16sp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="32dp"
        app:layout_constraintEnd_toStartOf="@+id/dealCurrencyTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dealTitleTextView"/>

    <TextView
        android:id="@+id/dealCurrencyTextView"
        android:text="@string/br_currency"
        android:textSize="12sp"
        android:textColor="#FF4500"
        app:layout_constraintTop_toBottomOf="@+id/dealImageView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/dealPriceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
    </TextView>

    <TextView
        android:id="@+id/dealPriceTextView"
        android:text="9.9"
        android:textColor="#FF4500"
        android:textSize="18sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/dealImageView"/>

</androidx.constraintlayout.widget.ConstraintLayout>